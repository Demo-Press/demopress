# DemoPress marketing site

Dark marketing implementation for `demopress.co.uk`, built around the actual capabilities of DemoPress Core.

```bash
npm install
npm run dev
```

The site is intentionally deployment-neutral until the production host/repository is confirmed. `npm run build` outputs a static site to `dist/`.

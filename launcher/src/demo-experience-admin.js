const express=require('express');
const settings=require('./settings');
const {adminPage}=require('./ui');
const {esc}=require('./helpers');
const r=express.Router();
function on(key,def='0'){return settings.get(key,def)==='1'}
r.get('/settings/demo-experience',(req,res)=>res.send(adminPage('Demo Experience',`<div class="crumb">Manager / Settings / Demo Experience</div><h1>Demo Experience</h1><div class="sub">Add a lightweight DemoPress toolbar and visitor guidance to disposable WordPress demos without modifying the product being demonstrated.</div><form method="post" action="/manage/settings/demo-experience"><div class="grid"><div class="card"><h2 style="margin-top:0">Toolbar</h2><label>Demo toolbar<select name="toolbar_enabled"><option value="1" ${on('demo_toolbar_enabled','1')?'selected':''}>Enabled</option><option value="0" ${!on('demo_toolbar_enabled','1')?'selected':''}>Disabled</option></select></label><label>Show remaining time<select name="show_remaining"><option value="1" ${on('demo_show_remaining','1')?'selected':''}>Show</option><option value="0" ${!on('demo_show_remaining','1')?'selected':''}>Hide</option></select></label><label>Hide WordPress admin notices<select name="hide_notices"><option value="1" ${on('demo_hide_notices','0')?'selected':''}>Hide for demo users</option><option value="0" ${!on('demo_hide_notices','0')?'selected':''}>Leave unchanged</option></select></label></div><div class="card"><h2 style="margin-top:0">Visitor actions</h2><label>Exit / product URL<input type="url" name="exit_url" value="${esc(settings.get('demo_exit_url',''))}" placeholder="https://example.com"><span class="fieldhint">Optional external destination shown as Exit Demo.</span></label><label>Documentation URL<input type="url" name="docs_url" value="${esc(settings.get('demo_docs_url',''))}" placeholder="https://example.com/docs"><span class="fieldhint">Optional product documentation link.</span></label><label>CTA label<input name="cta_label" maxlength="40" value="${esc(settings.get('demo_cta_label',''))}" placeholder="Get the product"></label><label>CTA URL<input type="url" name="cta_url" value="${esc(settings.get('demo_cta_url',''))}" placeholder="https://example.com"></label></div></div><div class="card"><div class="label">Safety</div><h2>Product-neutral by design</h2><p class="muted">The toolbar is injected by DemoPress only for disposable demo users. It does not change the golden template or add product-specific behaviour. Platform-protected actions continue to be enforced independently.</p></div><button class="btn" type="submit">Save Demo Experience</button></form>`,'settings'))));
r.post('/settings/demo-experience',(req,res)=>{const bool=k=>req.body[k]==='1'?'1':'0',url=k=>{const v=String(req.body[k]||'').trim();if(!v)return'';const u=new URL(v);if(!['http:','https:'].includes(u.protocol))throw new Error('Only HTTP/HTTPS URLs are allowed');return u.toString()};try{settings.set('demo_toolbar_enabled',bool('toolbar_enabled'));settings.set('demo_show_remaining',bool('show_remaining'));settings.set('demo_hide_notices',bool('hide_notices'));settings.set('demo_exit_url',url('exit_url'));settings.set('demo_docs_url',url('docs_url'));settings.set('demo_cta_label',String(req.body.cta_label||'').trim().slice(0,40));settings.set('demo_cta_url',url('cta_url'));res.redirect('/manage/settings/demo-experience?saved='+encodeURIComponent('Demo experience saved'))}catch(e){res.redirect('/manage/settings/demo-experience?error='+encodeURIComponent(e.message))}});
module.exports=r;

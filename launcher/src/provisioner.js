const profile=require("./profile"),path=require("path"),docker=require("./docker"),db=require("./database"),config=require("./config"),settings=require("./settings"),snapshots=require("./snapshots"),actions=require("./docker-actions"),{v4:uuid}=require("uuid");const sleep=ms=>new Promise(r=>setTimeout(r,ms));
function event(id,stage,message,level="info"){db.prepare("INSERT INTO events(demo_id,created_at_ms,stage,level,message) VALUES(?,?,?,?,?)").run(id,Date.now(),stage,level,message);console.log(`[${new Date().toISOString()}] [${id}] [${stage}] ${message}`)}function stage(id,s,m,status="provisioning"){db.prepare("UPDATE demos SET status=?,provision_stage=?,status_message=? WHERE id=?").run(status,s,m,id)}
function shq(v){return `'${String(v).replace(/'/g,"'\\''")}'`}
async function dbReady(c,id,dbName,user,pass){
  const started=Date.now();
  for(let i=0;i<60;i++){
    /*
     * Never allow one client connection attempt to block provisioning.
     * MariaDB's entrypoint briefly starts a socket-only temporary server,
     * then restarts on TCP 3306. A client invoked during that transition can
     * otherwise sit inside docker exec indefinitely on some hosts.
     */
    const cmd=`timeout -k 1s 4s mariadb --connect-timeout=2 --protocol=tcp -h127.0.0.1 -P3306 -u${shq(user)} -p${shq(pass)} ${shq(dbName)} -Nse 'SELECT 1' </dev/null`;
    let r={code:124,out:"probe timed out"};
    try{r=await actions.exec(c,cmd)}catch(e){r={code:125,out:e.message||String(e)}}
    if(r.code===0&&String(r.out).trim().endsWith("1")){
      event(id,"database",`MariaDB ready after ${((Date.now()-started)/1000).toFixed(2)}s`);
      return;
    }
    if(i===0||i%5===0){
      const detail=String(r.out||"").trim().split("\n").slice(-1)[0]||`exit ${r.code}`;
      event(id,"database",`Still waiting for MariaDB (${((Date.now()-started)/1000).toFixed(1)}s): ${detail.slice(0,180)}`,"debug");
    }
    await sleep(1000);
  }
  throw new Error("MariaDB readiness timeout: demo database user could not connect to port 3306");
}
function labels(id,kind){return{"com.demopress.demo":"true","com.demopress.instance":config.instanceId,"com.demopress.demo.id":id,"com.demopress.kind":kind,"com.demopress.product":config.profile}}
async function routeCheck(url){for(let i=0;i<6;i++){try{const r=await fetch(url,{redirect:"manual"});if(r.status>=200&&r.status<500)return{ok:true,status:r.status}}catch(_){}await sleep(1000)}return{ok:false,status:0}}
async function provision(id,{reset=false}={}){let wp=null,dbc=null;const d=db.prepare("SELECT * FROM demos WHERE id=?").get(id),snap=snapshots.current();try{if(!snap)throw new Error("No published golden snapshot");stage(id,"database","Starting isolated database");const dbName=`wp_${id.replace(/-/g,"")}`,user=`user_${id.replace(/-/g,"")}`,root=uuid(),pass=uuid(),t0=Date.now();dbc=await docker.createContainer({Image:"mariadb:11",name:`${id}-db`,Env:[`MARIADB_DATABASE=${dbName}`,`MARIADB_USER=${user}`,`MARIADB_PASSWORD=${pass}`,`MARIADB_ROOT_PASSWORD=${root}`],Labels:labels(id,"database"),HostConfig:{NetworkMode:config.network,RestartPolicy:{Name:"no"}}});await dbc.start();db.prepare("UPDATE demos SET db_container_id=? WHERE id=?").run(dbc.id,id);event(id,"database",`MariaDB container started: ${dbc.id.slice(0,12)}`);stage(id,"database","Waiting for isolated database");await dbReady(dbc,id,dbName,user,pass);db.prepare("UPDATE demos SET database_ms=? WHERE id=?").run(Date.now()-t0,id);stage(id,"template","Importing published snapshot");const ti=Date.now();await actions.importSql(dbc,root,dbName,path.join(snap.path,"database.sql"));db.prepare("UPDATE demos SET import_ms=?,template_version=? WHERE id=?").run(Date.now()-ti,snap.version,id);event(id,"template","Snapshot database import completed");stage(id,"wordpress","Creating WordPress container");const tw=Date.now(),image=await docker.demopressEnsureImage();event(id,"wordpress",`Using demo image ${image.Id.slice(0,19)}`);const bind=`${snap.path}:/snapshot:ro`;wp=await docker.createContainer({Image:config.demoImage,name:id,Env:[`WORDPRESS_DB_HOST=${id}-db:3306`,`WORDPRESS_DB_USER=${user}`,`WORDPRESS_DB_PASSWORD=${pass}`,`WORDPRESS_DB_NAME=${dbName}`,`DEMOPRESS_MODE=demo`,`DEMOPRESS_DEMO_URL=${d.url}`,`DEMOPRESS_DEMO_USER=${d.admin_user}`,`DEMOPRESS_DEMO_PASSWORD=${d.admin_password}`,`DEMOPRESS_REQUIRED_PLUGINS=${(profile.requiredPlugins||[]).join(",")}`,`DEMOPRESS_REQUIRED_THEME=${profile.requiredTheme||""}`],Labels:{...labels(id,"wordpress"),"traefik.enable":"true",[`traefik.http.routers.${id}.rule`]:`Host(\`${id}.${config.domain}\`)`,[`traefik.http.routers.${id}.entrypoints`]:"https",[`traefik.http.routers.${id}.tls`]:"true",[`traefik.http.routers.${id}.tls.certresolver`]:config.certResolver,[`traefik.http.services.${id}.loadbalancer.server.port`]:"80"},HostConfig:{NetworkMode:config.network,Binds:[bind],Memory:536870912,NanoCpus:500000000,RestartPolicy:{Name:"no"}}});await wp.start();db.prepare("UPDATE demos SET container_id=?,wordpress_ms=? WHERE id=?").run(wp.id,Date.now()-tw,id);stage(id,"finalising","Personalising your demo");const tf=Date.now();const fin=await actions.exec(wp,"/usr/local/bin/finalise-clone.sh");if(fin.code!==0)throw new Error(`Clone finalisation failed: ${fin.out.slice(-3000)}`);db.prepare("UPDATE demos SET finalise_ms=? WHERE id=?").run(Date.now()-tf,id);event(id,"finalising","WordPress verification passed");stage(id,"routing","Preparing secure URL");const tr=Date.now(),pub=await routeCheck(d.url);if(!pub.ok)throw new Error("Public HTTPS route did not become ready");db.prepare("UPDATE demos SET routing_ms=?,provision_finished_at=?,status='running',provision_stage='ready',status_message='Your demo is ready',error_message=NULL,health_status='healthy',last_health_at=?,public_route_status='verified' WHERE id=?").run(Date.now()-tr,Math.floor(Date.now()/1000),Math.floor(Date.now()/1000),id);event(id,"ready","Demo provisioning completed successfully");}catch(e){event(id,"failed",e.message,"error");const l=(wp?await actions.logs(wp):"")+"\n--- DATABASE ---\n"+(dbc?await actions.logs(dbc):"");db.prepare("UPDATE demos SET status='failed',provision_stage='failed',status_message='Provisioning failed',error_message=?,failure_logs=?,provision_finished_at=? WHERE id=?").run(e.message,l,Math.floor(Date.now()/1000),id)}}
function create(ip,{adminTest=false}={}){if(!adminTest&&settings.get("maintenance_mode","0")==="1")throw new Error("Demo maintenance is active.");if(!snapshots.current())throw new Error("No golden template has been published yet.");const active=db.prepare("SELECT COUNT(*) c FROM demos WHERE status IN ('queued','provisioning','running','resetting')").get().c;if(active>=settings.number("max_active_demos",config.maxDemos))throw new Error("All demo slots are currently in use.");const id="demo-"+uuid().slice(0,8),now=Math.floor(Date.now()/1000),idle=settings.number("idle_lifetime",config.lifetime),hard=settings.number("max_lifetime",config.maxLifetime),user=`demo_${id.replace(/-/g,"")}`,pass=uuid(),url=`https://${id}.${config.domain}`,snap=snapshots.current();db.prepare("INSERT INTO demos(id,url,created_at,last_activity,expires_at,hard_expires_at,status,provision_stage,status_message,admin_user,admin_password,ip_address,template_version,provision_started_at,demo_type,platform_version) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)").run(id,url,now,now,now+idle,now+hard,"queued","queued",adminTest?"Preparing administrator test demo":"Preparing your demo",user,pass,adminTest?"admin-test":ip,snap.version,now,adminTest?"admin_test":"public",config.platformVersion);event(id,"queued",adminTest?"Administrator test launch requested":"Public demo launch requested");setImmediate(()=>provision(id));return{id,url}}
module.exports={create,provision};

const express=require("express");const snapshots=require("./snapshots");const provisioner=require("./provisioner");const {adminPage}=require("./ui");const {esc}=require("./helpers");const r=express.Router();
r.get("/",async(req,res)=>{let status={},validation={};try{status=await snapshots.status()}catch(e){status={ok:false,error:e.message}}try{validation=await snapshots.validate()}catch(e){validation={ok:false,error:e.message}}const list=snapshots.list(),cur=snapshots.current();res.send(adminPage("Template",`<div class="crumb">Manager / Template</div><h1>Golden Template</h1><div class="sub">Validate, publish, test and roll back the source used for new demos.</div><div class="grid"><div class="card"><div class="label">Connection</div><div class="stat ${status.ok?"success":"danger"}">${status.ok?"Connected":"Unavailable"}</div></div><div class="card"><div class="label">Validation</div><div class="stat ${validation.ok?"success":"danger"}">${validation.ok?"Passed":"Failed"}</div></div><div class="card"><div class="label">Current</div><div class="value">${esc((cur||{}).version||"None")}</div><p class="muted">${cur?(cur.size_bytes/1048576).toFixed(1)+" MB total":"No published snapshot"}</p></div></div><div class="actions"><form method="post" action="/manage/template/publish"><button class="btn">Validate & Publish Snapshot</button></form>${cur?'<form method="post" action="/manage/template/test"><button class="btn secondary">Launch Test From Snapshot</button></form>':""}</div><h2>Validation checks</h2><div class="codeblock">${esc(JSON.stringify(validation,null,2))}</div><h2>Snapshots</h2><div class="tablewrap"><table><tr><th>Version</th><th>Created</th><th>Size</th><th>State</th><th>Actions</th></tr>${list.map(s=>`<tr><td>${esc(s.version)}</td><td>${new Date(s.created_at*1000).toLocaleString()}</td><td>${(s.size_bytes/1048576).toFixed(1)} MB</td><td>${s.is_current?"CURRENT":""}</td><td><div class="actions" style="margin:0">${!s.is_current?`<form method="post" action="/manage/template/${esc(s.version)}/restore"><button class="btn mini secondary">Restore</button></form><form method="post" action="/manage/template/${esc(s.version)}/delete"><button class="btn mini red">Delete</button></form>`:""}</div></td></tr>`).join("")}</table></div>`,"template"))});
r.post("/publish",async(req,res)=>{try{await snapshots.publish();res.redirect("/manage/template")}catch(e){res.status(500).send(adminPage("Publish failed",`<h1>Snapshot failed</h1><div class="notice">${esc(e.message)}</div>`,"template"))}});
r.post("/test",(req,res)=>{try{const d=provisioner.create("admin-template-test",{adminTest:true});res.redirect(`/manage/demos/${d.id}`)}catch(e){res.status(500).send(adminPage("Test failed",`<h1>Snapshot test failed</h1><div class="notice">${esc(e.message)}</div>`,"template"))}});
r.post("/:version/restore",(req,res)=>{snapshots.setCurrent(req.params.version);res.redirect("/manage/template")});r.post("/:version/delete",(req,res)=>{snapshots.remove(req.params.version);res.redirect("/manage/template")});module.exports=r;

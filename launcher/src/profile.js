const fs=require("fs"),path=require("path"),config=require("./config");function load(){const f=path.join(config.profileRoot,`${config.profile}.json`);if(!fs.existsSync(f))throw new Error(`DemoPress profile not found: ${config.profile}`);return JSON.parse(fs.readFileSync(f,"utf8"))}module.exports=load();

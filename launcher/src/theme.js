const fs=require("fs"),path=require("path"),config=require("./config");function safe(name){return /^[a-z0-9_-]+$/i.test(name)?name:"default"}function load(){let name=safe(config.theme);let dir=path.join(config.themeRoot,name);if(!fs.existsSync(path.join(dir,"theme.json"))){name="default";dir=path.join(config.themeRoot,name)}return{name,config:JSON.parse(fs.readFileSync(path.join(dir,"theme.json"),"utf8")),css:fs.existsSync(path.join(dir,"theme.css"))?fs.readFileSync(path.join(dir,"theme.css"),"utf8"):""}}module.exports={load};

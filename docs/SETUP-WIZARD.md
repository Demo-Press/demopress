# Setup Wizard
1. Product identity
2. Domains
3. Branding
4. Plugin/theme inventory
5. DNS + Agent readiness
6. Publish/test/go-live

Profile changes are saved to `PROFILE_PATH` (default `/data/profile.json`) and are read dynamically without restarting the launcher.
